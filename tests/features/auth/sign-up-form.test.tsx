/**
 * Example test for a future auth feature.
 * Replace with real component imports once features/auth/ is scaffolded.
 */

describe("SignUpForm", () => {
  it.todo("renders name, email, and password fields");
  it.todo("shows validation errors on empty submit");
  it.todo("calls signUp with correct payload on valid submit");
  it.todo("redirects after successful sign-up");
});
